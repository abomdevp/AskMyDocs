services:
  rag-api:
    build: .
    container_name: rag-pdf-api
    ports:
      - "8000:8000"
    environment:
      OLLAMA_HOST: http://host.docker.internal:11434
    volumes:
      - ./data:/app/data